# 智能网页信息提取器 — 业务需求规格说明书 (PRD)

> 版本：v1.0  
> 日期：2026-06-29  
> 状态：已确认，进入技术设计阶段

---

## 目录

1. [产品概述](#1-产品概述)
2. [目标用户](#2-目标用户)
3. [核心价值主张](#3-核心价值主张)
4. [功能需求清单](#4-功能需求清单)
5. [功能详细说明](#5-功能详细说明)
6. [非功能需求](#6-非功能需求)
7. [用户旅程地图](#7-用户旅程地图)
8. [浏览器兼容性](#8-浏览器兼容性)
9. [决策记录](#9-决策记录)
10. [UI/UE/UX 设计规范 — 悬浮边沿面板](#10-uiueux-设计规范--悬浮边沿面板)
11. [验收清单](#11-验收清单)

---

## 1. 产品概述

### 1.1 产品名称

**智能网页信息提取器 (Smart Web Extractor)**

### 1.2 一句话描述

一款 AI 驱动的浏览器插件，帮助用户在 30 秒内从冗长网页中提取干货精华——去广告、取正文、收图片、析表格，一键生成结构化摘要并导出。

### 1.3 产品定位

传统「稍后阅读」的替代方案。当用户面对信息密度低、阅读时间成本高的网页时，插件利用 AI 能力快速解析内容、浓缩要点，让用户花 1/10 的时间获取 90% 的信息价值。

---

## 2. 目标用户

| 用户画像 | 典型场景 | 核心痛点 |
|----------|----------|----------|
| **知识工作者** | 每日浏览大量行业文章、报告、新闻 | 信息过载，超 80% 长文加入收藏夹后再也不看 |
| **内容创作者** | 调研素材、收集资料用于写作 | 需要快速提取多篇文章的核心观点和数据 |
| **电商/数据运营** | 监控竞品价格、行业榜单、行情数据 | 网页表格和图片数据难以结构化导出 |
| **学生/研究人员** | 查阅论文、学术资料、课程文档 | 需要在大量文献中快速筛选和整理关键信息 |

---

## 3. 核心价值主张

```
传统方式：打开网页 → 阅读 10 分钟 → 手动摘抄笔记 → 存入收藏夹吃灰
插件方式：打开网页 → 触发提取 → AI 30 秒生成摘要 → 导出带走
```

| 价值维度 | 描述 |
|----------|------|
| **效率提升** | 阅读时间缩短 80%-90% |
| **信息留存** | 结构化摘要 + 多格式导出（MD/TXT/Word），告别收藏夹 |
| **智能理解** | AI 综合正文 + 图片内容，生成有质量的摘要，非简单截取 |
| **用户可控** | 自定义提取规则、摘要模板、浓缩程度、导出格式 |

---

## 4. 功能需求清单

### 4.1 功能模块总览

```
┌─────────────────────────────────────────────────────┐
│               智能网页信息提取器 v1.0                  │
├───────────────┬───────────────┬─────────────────────┤
│   提取能力层   │    AI 智能层   │    交互与输出层      │
├───────────────┼───────────────┼─────────────────────┤
│ F1 正文提取   │ F5 AI 摘要    │ F9  结果预览编辑     │
│ F2 图片提取   │ F6 预设模板   │ F10 多格式导出      │
│ F3 表格提取   │ F7 自定义规则 │ F11 多触发方式      │
│ F4 广告过滤   │ F8 站点规则   │ F12 双语界面        │
└───────────────┴───────────────┴─────────────────────┘
```

### 4.2 完整功能列表

| 编号 | 功能模块 | 功能点 | 优先级 |
|------|----------|--------|--------|
| F1 | 正文提取 | 自动识别页面正文内容区域 | P0 |
| F1.1 | 正文提取 | 支持用户光标框选指定区域提取 | P0 |
| F1.2 | 正文提取 | 支持整页提取 | P0 |
| F2 | 图片提取 | 自动聚合页面图片，缩略图展示 | P0 |
| F2.1 | 图片提取 | 按类型/尺寸分类筛选 | P1 |
| F2.2 | 图片提取 | AI 生成每张图片的一句话描述 | P1 |
| F2.3 | 图片提取 | 勾选后打包 ZIP 下载（跨域图片经 Service Worker 中转，无法获取的跳过并提示） | P1 |
| F3 | 表格提取 | 自动识别 `<table>` 及类表格结构 | P0 |
| F3.1 | 表格提取 | 导出为 CSV/Excel | P1 |
| F3.2 | 表格提取 | AI 对表格数据生成分析摘要 | P1 |
| F4 | 广告过滤 | 提取时自动识别并排除广告区域 | P0 |
| F4.1 | 广告过滤 | 提供"净化页面预览"模式，用户可微调 | P1 |
| F4.2 | 广告过滤 | 静默过滤 / 预览净化 双模式可切换 | P1 |
| F5 | AI 摘要 | 调用 DeepSeek / 通义千问 API 生成摘要 | P0 |
| F5.1 | AI 摘要 | 支持用户自带 API Key | P0 |
| F5.2 | AI 摘要 | API Key 加密本地存储，可选浏览器同步 | P0 |
| F6 | 预设模板 | 文字摘要模板（3-5 句话概括） | P0 |
| F6.1 | 预设模板 | 结构化摘要模板（观点/论据/数据/结论） | P0 |
| F6.2 | 预设模板 | 图表化摘要模板（思维导图/Mermaid） | P1 |
| F6.3 | 预设模板 | 用户可自定义摘要模板（类似自定义 prompt） | P0 |
| F7 | 自定义规则 | 用户自定义提取 prompt 模板 | P0 |
| F7.1 | 自定义规则 | 按站点绑定不同规则和偏好 | P1 |
| F7.2 | 自定义规则 | 规则导入/导出/分享（JSON 格式） | P1 |
| F8 | 预设场景 | 新闻/博客文章提取 | P0 |
| F8.1 | 预设场景 | 产品页面提取（名称/价格/规格/评价） | P1 |
| F8.2 | 预设场景 | 学术论文提取（摘要/方法/结论/参考） | P1 |
| F8.3 | 预设场景 | 社交媒体帖子提取 | P1 |
| F8.4 | 预设场景 | 视频页面提取（标题/简介/评论摘要） | P1 |
| F8.5 | 预设场景 | 办公文档提取 | P2 |
| F8.6 | 预设场景 | 电商榜单提取 | P1 |
| F8.7 | 预设场景 | 行情数据提取 | P1 |
| F9 | 结果预览编辑 | 富文本 Markdown 可视化编辑 | P0 |
| F9.1 | 结果预览编辑 | 对话式 AI 调整（自然语言让 AI 改摘要） | P1 |
| F9.2 | 结果预览编辑 | 支持增删改摘要块 | P1 |
| F10 | 多格式导出 | TXT 纯文本导出 | P0 |
| F10.1 | 多格式导出 | Markdown 导出 | P0 |
| F10.2 | 多格式导出 | Word (.doc 兼容格式) 导出 | P0 |
| F10.3 | 多格式导出 | 用户可单选或多选导出格式 | P0 |
| F11 | 触发方式 | 工具栏图标 Popup 弹窗 | P0 |
| F11.1 | 触发方式 | 右键菜单（页面/选中区域） | P0 |
| F11.2 | 触发方式 | 页面浮动按钮 | P1 |
| F11.3 | 触发方式 | 键盘快捷键（默认 Ctrl+Shift+E，修改需前往浏览器快捷键设置页） | P1 |
| F11.4 | 触发方式 | 设置页可开关各触发方式 | P1 |
| F12 | 界面语言 | 中文简体 | P0 |
| F12.1 | 界面语言 | 英文 | P1 |
| F12.2 | 界面语言 | 设置页切换语言 | P1 |

---

## 5. 功能详细说明

### 5.1 F1 正文提取

**输入**：当前浏览器活动标签页的 DOM

**处理逻辑**：
1. 基于文本密度算法计算各 DOM 节点的"内容得分"
2. 识别最高分区域作为正文容器
3. 排除得分异常低的区域（侧栏、导航、页脚）
4. 保留正文中的标题层级结构（h1-h6）
5. 若用户光标框选，则仅处理选中区域

**输出**：结构化正文对象（标题、段落数组、元信息）

**验收标准**：
- 主流新闻/博客网站正文识别准确率 ≥ 85%
- 提取耗时 < 500ms（不含 AI 调用）

### 5.2 F2 图片提取

**处理逻辑**：
1. 从正文区域收集所有 `<img>` 元素
2. 过滤：排除 < 50×50px 的图标、base64 内联像素图
3. 分类：配图 / 图表 / Logo / 图标
4. 分类依据：alt 属性、尺寸、CSS 类名、周围文本
5. 可选 AI 生成图片描述（将图片转为 base64 发给视觉模型或根据上下文推断）

**输出**：图片列表（URL、尺寸、分类、AI 描述）

### 5.3 F3 表格提取

**处理逻辑**：
1. 遍历页面所有 `<table>` 元素
2. 检测 `<th>` / `<thead>` 识别表头
3. 对 `<div>` 模拟的表格结构进行启发式识别（`display: table`、grid 布局）
4. 数据清洗：合并单元格处理、空值填充
5. AI 分析：将表格前 N 行作为样本发给 AI，生成数据摘要

**输出**：表格数据（二维数组 + 表头映射）+ AI 分析结果

### 5.4 F4 广告过滤

**实现策略**：提取时过滤（非拦截式，页面正常加载广告，仅在提取结果中排除）

**广告识别规则**：
1. **CSS 类名/ID 黑名单**：匹配 `ad`、`ads`、`advertisement`、`banner`、`sponsored`、`promo` 等
2. **iframe 检测**：小尺寸 iframe（典型广告尺寸）标记为可疑
3. **文本密度异常**：文本极少但链接/图片密集的区块
4. **固定定位弹窗**：`position: fixed` 且包含关闭按钮的覆盖层

**双模式**：
- **静默过滤**（默认）：后台自动排除，用户只看结果
- **预览净化**：高亮标记疑似广告区域，用户可点击排除/恢复

### 5.5 F5 AI 摘要

**支持供应商**：
| 供应商 | API 端点 | 模型 |
|--------|----------|------|
| DeepSeek | `api.deepseek.com/v1` | deepseek-chat |
| 通义千问 | `dashscope.aliyuncs.com/compatible-mode/v1` | qwen-plus |

**API Key 管理**：
- 加密存储于 `chrome.storage.local`
- 加密算法：Web Crypto API (AES-GCM)，密钥派生自设备指纹
- 可选通过 `chrome.storage.sync` 跨设备同步（加密后的 Key）

**摘要生成流程**：
```
原始内容 + 图片描述 + 表格数据
        │
        ▼
   [用户选择的模板 prompt]
        │
        ▼
   [AI API 调用]
        │
        ▼
   结构化摘要结果 (Markdown)
```

**错误处理**：
- API Key 无效 → 提示用户检查配置
- 网络超时（30s）→ 提示重试，建议检查网络
- 内容过长 → 自动截断至模型上下文窗口上限
- 速率限制 → 显示等待时间

### 5.6 F6 预设模板

**模板 1 — 文字摘要（默认）**：
```
请用 3-5 句话概括以下网页内容的核心要点，语言简洁凝练。
---
{content}
```

**模板 2 — 结构化摘要**：
```
请按以下结构提取网页内容：
## 核心观点
## 关键论据
## 重要数据
## 结论/启示
---
{content}
```

**模板 3 — 图表化摘要**：
```
请将以下内容转化为 Mermaid 思维导图（mindmap）语法：
---
{content}
```

**模板 4 — 自定义**：用户自由编辑 prompt 模板，支持变量 `{content}`、`{title}`、`{url}`、`{images}`、`{tables}`、`{lang}`

### 5.7 F7 自定义规则

**规则定义格式 (JSON)**：
```json
{
  "name": "我的规则",
  "description": "提取知乎文章核心观点",
  "urlPattern": "*://*.zhihu.com/*",
  "prompt": "请提取以下知乎文章的...",
  "extractionMode": "fullPage | selection",
  "summaryLength": "short | medium | long",
  "includeImages": true,
  "includeTables": true
}
```

**规则链**（管道式处理）：
```
[提取] → [过滤去噪] → [字段提取] → [AI总结] → [格式化输出]
```

每一步可独立配置策略。

**规则分享**：导出为 `.json` 文件，他人可导入。社区规则市场后续版本支持。

### 5.8 F9 结果预览编辑

**编辑器能力**：
- 可视化 Markdown 工具栏：加粗、斜体、标题、列表、引用、链接
- 实时预览 / 源码切换
- 对话式 AI 调整：底部输入框，用自然语言让 AI 修改（如"把第二段精简一下""把数据部分用表格展示"）

### 5.9 F10 多格式导出

| 格式 | 实现方式 | 保留内容 |
|------|----------|----------|
| **TXT** | 纯文本，去除 Markdown 标记 | 纯文字 |
| **MD** | 直接保存 Markdown 源码 | 标题层级、列表、链接、图片引用、代码块、表格 |
| **Word** | HTML → Blob → .doc（Word 兼容 HTML 格式，非真正 .docx/OOXML） | 基本排版、图片嵌入、表格；复杂排版可能部分丢失 |

> **Word 导出说明**：采用「HTML 转 Word 兼容格式（.doc）」方案，重命名 HTML 文件头即可被 Microsoft Word 直接打开，优点是零依赖、无需 docx 生成库。局限是无法生成真正的 .docx（OOXML）文件，复杂排版（嵌套表格、矢量图形、特殊页眉页脚）可能有限度。若后续需要真正的 .docx，需评估引入轻量 docx 生成库的成本（与零依赖约束冲突）。

导出时用户可勾选需要的格式（多选），点击导出后依次下载。

---

## 6. 非功能需求

### 6.1 性能

| 指标 | 目标值 |
|------|--------|
| 页面内容提取耗时 | < 500ms |
| 广告过滤耗时 | < 200ms |
| AI 摘要生成（不含网络） | 取决于 API |
| 插件启动/弹窗打开 | < 300ms |
| 内存占用（空闲） | < 50MB |

### 6.2 安全性

#### 6.2.1 API Key 安全（最高优先级）

| 安全维度 | 具体要求 |
|----------|----------|
| **存储隔离** | 每个 API Key 以独立加密键存储，不同供应商的 Key 使用不同加密 IV，互不关联 |
| **用户隔离** | API Key 仅存储在用户本地浏览器 `chrome.storage.local`，不允许上传至任何远程服务器。代码中不存在任何向第三方（含插件开发者服务器）发送 API Key 的逻辑路径 |
| **脱敏显示** | UI 中 API Key 永远以脱敏形式展示：仅显示首 4 位 + 尾 4 位，中间以 `****` 替代（如 `sk-a1****9z2x`）。完整 Key 仅在内存中短暂持有，用完即释放 |
| **加密算法** | 使用 Web Crypto API 的 AES-GCM 256 位加密。加密密钥通过 PBKDF2 从设备指纹派生（迭代次数 ≥ 100,000），每个 Key 使用独立随机 IV（12 字节） |
| **内存安全** | API Key 在 JS 内存中的生命周期 ≤ API 调用持续时间。调用完成后立即将变量置 `null`，不缓存明文 Key |
| **日志安全** | 禁止在任何 console.log / error 输出中打印 API Key 明文或密文。错误消息中涉及 Key 的部分统一显示为 `[API_KEY]` 占位符 |
| **同步控制** | API Key **默认不同步**至 `chrome.storage.sync`。若用户主动开启同步（需二次确认弹窗提醒风险），同步的仅为加密后的密文，且加密密钥（设备指纹派生）不同步，确保其他设备无法解密 |
| **导出排除** | 规则/配置导出（JSON 文件）中，API Key 字段永远为空字符串，不随配置导出 |
| **卸载清除** | 插件卸载时，`chrome.storage.local` 中所有 `secure:*` 前缀的键随扩展卸载自动清除（浏览器行为） |

#### 6.2.2 数据传输安全

- 插件纯前端运行，不经过任何第三方服务器
- 用户内容仅通过 HTTPS 发送至用户自己配置的 AI API 端点（DeepSeek / 通义千问）
- 请求头中 `Authorization: Bearer {key}` 仅存在于 HTTPS 请求中
- CSP 配置仅允许连接 `api.deepseek.com` 和 `dashscope.aliyuncs.com`

#### 6.2.3 权限最小化

- manifest.json 申请最小权限原则
- 不申请 `cookies`、`webRequest`、`history`、`bookmarks` 等无关权限
- **host_permissions 说明**：边沿标签需在用户打开的任意页面常驻注入 Content Script，无法仅凭 `activeTab` 实现（`activeTab` 仅在用户主动操作后生效，无法满足"打开页面即显示边沿标签"）。因此采用 `optional_host_permissions: ["<all_urls>"]`，在用户首次使用时（首次点击工具栏图标或安装引导）申请，避免安装期即索取全部权限；用户拒绝时降级为"仅点击图标后注入"的按需模式
- AI 调用、文件下载、跨域图片获取等需要网络与下载能力的操作，统一经 Service Worker 中转执行（详见技术架构设计 §4.1 与 §9.6）

### 6.3 可用性

- 无需注册/登录，安装即用
- 首次使用引导：提示配置 API Key
- 错误提示使用用户友好语言（非技术术语）
- 支持中英双语界面，跟随浏览器语言自动选择

### 6.4 可维护性

- 纯原生 JS/HTML/CSS，零 npm 安装、零构建步骤；ZIP 打包与 Markdown 渲染为内联「自实现」的轻量实现（非引入第三方库源码），代码可直接审查
- 模块化文件结构，每个模块职责单一
- 代码注释覆盖率 ≥ 30%

---

## 7. 用户旅程地图

> 设计理念：插件悬浮于浏览器右侧边沿，如一个随叫随到的智能助手。用户无需离开当前页面，一切操作在悬浮面板中完成。

### 7.1 用户旅程总览

```
┌──────────────────────────────────────────────────────────────────┐
│  阶段    │ 发现        │ 激活      │ 提取      │ 消费      │ 留存      │
├──────────────────────────────────────────────────────────────────┤
│  触点    │ 浏览器右侧   │ 边沿标签   │ 悬浮面板   │ 悬浮面板   │ 边沿标签   │
│          │ 边沿标签     │ 点击展开   │ 滑动展开   │ 结果展示   │ 再次出现   │
├──────────────────────────────────────────────────────────────────┤
│  情绪    │ 好奇 🤔     │ 惊喜 ✨   │ 期待 ⏳   │ 满足 😌   │ 依赖 💪   │
├──────────────────────────────────────────────────────────────────┤
│  目标    │ "这是什么？" │ "试试看"  │ "帮我提取" │ "果然有用" │ "每天都用" │
└──────────────────────────────────────────────────────────────────┘
```

### 7.2 旅程一：首次用户（0→1 激活）

```
用户首次安装插件
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│ STEP 1: 发现 — 浏览器右侧出现一个精致的蓝色边沿标签           │
├─────────────────────────────────────────────────────────────┤
│ 用户打开任意网页                                              │
│   → 浏览器右侧边沿出现蓝色小标签 "🔍 提取"（半透明，不遮挡内容）│
│   → 鼠标悬停时标签微微放大，显示提示 "点击提取网页精华"        │
│   → 用户好奇，点击标签                                        │
└─────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│ STEP 2: 激活 — 悬浮面板从右侧滑出                             │
├─────────────────────────────────────────────────────────────┤
│ 面板从浏览器右边缘平滑滑出（300ms ease-out 动画）             │
│ 面板宽 420px，覆盖在页面上方，内容区半透明遮罩                 │
│                                                              │
│ 面板内显示：                                                  │
│  ┌──────────────────────────────────┐                       │
│  │  🔮 智能网页提取器          [×]  │  ← 头部                │
│  │──────────────────────────────────│                       │
│  │                                  │                       │
│  │  🎉 欢迎使用！                   │                       │
│  │                                  │                       │
│  │  我是你的网页智能助手，           │                       │
│  │  帮你快速提取网页精华。           │                       │
│  │                                  │                       │
│  │  开始前需要配置 AI 服务：        │                       │
│  │  ┌──────────────────────────┐   │                       │
│  │  │ 选择 AI 供应商           │   │  ← 引导配置            │
│  │  │ ○ DeepSeek  ○ 通义千问   │   │                       │
│  │  │ [________________] API   │   │                       │
│  │  │ 🔒 加密存储在本地        │   │                       │
│  │  └──────────────────────────┘   │                       │
│  │                                  │                       │
│  │  [测试连接]  [稍后配置]         │                       │
│  └──────────────────────────────────┘                       │
└─────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│ STEP 3: 首次提取 — 体验 Aha Moment                           │
├─────────────────────────────────────────────────────────────┤
│ 用户输入 API Key → 测试连接成功 → 面板自动进入提取模式       │
│                                                              │
│ 顶部显示当前页面标题                                          │
│ 中部显示可选的摘要模板标签：                                  │
│   [文字摘要] [结构化摘要] [图表化摘要]                        │
│                                                              │
│ 用户点击大大的蓝色按钮 "⚡ 开始提取"                          │
│                                                              │
│ 面板显示进度动画：                                            │
│  ✅ 识别正文内容 (0.3s)                                      │
│  ✅ 过滤广告元素 (0.1s)                                      │
│  ✅ 收集图片 5 张 (0.2s)                                     │
│  ⏳ AI 正在分析并生成摘要... (3-8s)                          │
│     └─ 进度条 + "正在连接 DeepSeek..."                       │
│                                                              │
│  → 摘要出现，用户看到干净的结构化内容                          │
│  → 内心："原来这篇文章讲的是这个！"  ← Aha Moment             │
└─────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│ STEP 4: 消费结果 — 阅读、编辑、导出                           │
├─────────────────────────────────────────────────────────────┤
│ 面板切换到结果视图：                                          │
│  ┌──────────────────────────────────┐                       │
│  │ 📄 正文  🖼 图片(5)  📊 表格(2) │  ← 标签切换            │
│  │──────────────────────────────────│                       │
│  │                                  │                       │
│  │ ## 核心观点                      │                       │
│  │ 本文主要讨论了...                │  ← Markdown 渲染       │
│  │                                  │                       │
│  │ ## 关键论据                      │                       │
│  │ 1. ...                          │                       │
│  │ 2. ...                          │                       │
│  │                                  │                       │
│  │ [✏️ 编辑] [📥 导出] [🔄 重提取] │  ← 底部操作栏          │
│  └──────────────────────────────────┘                       │
│                                                              │
│ 用户点击 "编辑" → 面板内切换为编辑模式（Markdown 编辑器）     │
│ 用户点击 "导出" → 底部弹出格式选择 → 下载文件                 │
└─────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│ STEP 5: 关闭 — 面板滑回，标签常驻                             │
├─────────────────────────────────────────────────────────────┤
│ 用户点击面板外区域 / 按 ESC / 点击 ×                         │
│   → 面板平滑滑回右边缘（250ms ease-in 动画）                  │
│   → 蓝色边沿标签保持显示                                      │
│   → 下次点击标签，面板再次滑出，回到上次的状态                 │
│                                                              │
│ 用户心理："下次看长文章再也不怕了"                            │
└─────────────────────────────────────────────────────────────┘
```

### 7.3 旅程二：日常高频使用（1→N 习惯养成）

```
场景：用户每天浏览 20+ 篇文章/报告/产品页

┌──────────────────────────────────────────────────────────────┐
│ 触点 1：看到长文章，下意识瞄一眼浏览器右侧                      │
├──────────────────────────────────────────────────────────────┤
│ → 边沿标签始终在那里，像一个安静的提醒                         │
│ → 用户点击标签，面板滑出                                      │
│ → 选择模板（或使用上次的默认模板）                             │
│ → 点击 "⚡ 开始提取"，3-5 秒后结果呈现                        │
│ → 扫一眼摘要，决定：                                          │
│    - 值得细读 → 点击原文链接回到页面                           │
│    - 有价值留存 → 导出 MD                                    │
│    - 不感兴趣 → 关闭面板，看下一篇                            │
│                                                              │
│ 整个过程：< 15 秒                                             │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│ 触点 2：选中一段文字，右键 → "智能提取选中内容"                 │
├──────────────────────────────────────────────────────────────┤
│ → 只提取选中部分，AI 针对性分析                               │
│ → 适合"只看这个产品的参数对比"这种局部需求                    │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│ 触点 3：按 Ctrl+Shift+E，面板直接滑出开始提取                  │
├──────────────────────────────────────────────────────────────┤
│ → 键盘党最爱，无需鼠标操作                                    │
│ → 面板自动使用默认模板和设置                                  │
│ → 3 秒后摘要呈现                                             │
└──────────────────────────────────────────────────────────────┘
```

### 7.4 旅程三：高级用户（规则与定制）

```
场景：用户频繁访问某类网站（如知乎、GitHub、电商平台），希望定制提取规则

┌──────────────────────────────────────────────────────────────┐
│ 发现需求                                                      │
├──────────────────────────────────────────────────────────────┤
│ 用户访问知乎文章，默认模板提取效果一般                         │
│ 用户想："知乎的文章结构固定，能不能每次都自动提取观点部分？"   │
└──────────────────────────────────────────────────────────────┘
    │
    ▼
┌──────────────────────────────────────────────────────────────┐
│ 创建规则                                                      │
├──────────────────────────────────────────────────────────────┤
│ 面板中点击 ⚙ 齿轮图标 → 面板切换到设置选项卡                  │
│ → [规则管理] → [+ 新建规则]                                  │
│ → 输入：                                                      │
│    名称："知乎文章提取"                                       │
│    URL 匹配：*://*.zhihu.com/*                                │
│    自定义 Prompt："提取该知乎文章的..."                       │
│ → [保存]                                                     │
└──────────────────────────────────────────────────────────────┘
    │
    ▼
┌──────────────────────────────────────────────────────────────┐
│ 规则自动生效                                                  │
├──────────────────────────────────────────────────────────────┤
│ 下次用户访问知乎文章 → 打开面板 → 自动识别并推荐该规则         │
│ → 一键提取 → 结果符合预期 → 用户满意                          │
│                                                              │
│ 用户将规则导出为 JSON → 分享给同事 → 同事导入即可用           │
└──────────────────────────────────────────────────────────────┘
```

### 7.5 旅程四：异常恢复

```
┌──────────────────────────────────────────────────────────────┐
│ 场景 A：API Key 过期或余额不足                                 │
├──────────────────────────────────────────────────────────────┤
│ 用户触发提取 → 面板显示进度 → 3 秒后报错                      │
│ 面板内显示：                                                  │
│  ┌──────────────────────────────────┐                       │
│  │ ⚠️ AI 调用失败                  │                       │
│  │                                  │                       │
│  │ DeepSeek 返回：余额不足          │                       │
│  │                                  │                       │
│  │ [前往 DeepSeek 充值]             │                       │
│  │ [更换 API Key]                   │                       │
│  │ [切换到通义千问]                 │                       │
│  └──────────────────────────────────┘                       │
│ → 用户不需要离开面板 → 面板内直接修改 Key 或切换供应商        │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│ 场景 B：页面无正文（纯图片页/视频页/空白页）                    │
├──────────────────────────────────────────────────────────────┤
│ 用户触发提取 → 0.5 秒后提示                                   │
│ 面板内显示：                                                  │
│  ┌──────────────────────────────────┐                       │
│  │ 📭 未检测到正文内容              │                       │
│  │                                  │                       │
│  │ 该页面可能为纯图片/视频/空白页   │                       │
│  │                                  │                       │
│  │ 建议：                          │                       │
│  │ • 尝试选中区域提取              │                       │
│  │ • 仅提取页面图片                │                       │
│  │                                  │                       │
│  │ [仅提取图片] [取消]             │                       │
│  └──────────────────────────────────┘                       │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│ 场景 C：网络断开                                              │
├──────────────────────────────────────────────────────────────┤
│ 提取阶段（本地）不受影响 → 正文/图片/表格正常提取              │
│ AI 分析阶段 → 请求失败 → 面板提示 "网络连接失败"               │
│ → 已提取的原始内容保留在面板中，用户可手动编辑后导出           │
│ → 网络恢复后一键 "重新分析" 即可                              │
└──────────────────────────────────────────────────────────────┘
```

### 7.6 情绪曲线

```
用户情绪值
  │
10├─                    ┌──────── 日常使用，稳定满意
  │                    ╱
 8├─          ┌──────╱──── 首次提取成功，Aha!
  │          ╱        
 6├─    ┌──╱────────── 配置完成
  │    ╱    
 4├─  ╱──────────────── 安装后首次看到标签
  │  ╱
 2├─╱────────────────── 打开浏览器（还未发现插件）
  │
  └┴──────────────────────────────────────────▶ 时间
    安装    首次点击   首次成功    第10次使用    第100次使用
```

### 7.7 核心流转路径总结

```
                    浏览器右侧边沿标签（始终可见）
                            │
                    点击 / 悬停 / 快捷键
                            │
                            ▼
              ┌─────────────────────────┐
              │   悬浮面板从右侧滑入      │ ← 一切操作在此完成
              │                         │
              │  ┌─────────────────┐    │
              │  │ Tab: 提取 │ 结果 │    │
              │  │ Tab: 图片 │ 表格 │    │
              │  │ Tab: 编辑 │ 导出 │    │
              │  │ Tab: 规则 │ 设置 │    │
              │  └─────────────────┘    │
              │                         │
              │  [⚡ 开始提取]           │
              │  [📥 导出] [✏️ 编辑]    │
              └─────────────────────────┘
                            │
              面板外点击 / ESC / ×
                            │
                            ▼
                    面板滑回右边缘
                    标签继续等待下次召唤
```

---

## 8. 浏览器兼容性

### 8.1 兼容范围

| 浏览器 | 内核 | 兼容策略 | 优先级 |
|--------|------|----------|--------|
| **Google Chrome** | Chromium | Manifest V3，主要开发目标 | P0 |
| **Microsoft Edge** | Chromium | 与 Chrome 完全兼容 | P0 |
| **夸克浏览器** | Chromium | 与 Chrome 一致，需测试 | P0 |
| **UC 浏览器** | Chromium | 与 Chrome 一致，需测试 | P0 |
| **Mozilla Firefox** | Gecko | 后续适配（Manifest V3 差异处理） | P1 |

### 8.2 技术基线

- **Manifest 版本**：V3（Chromium 系标准）
- **最小 Chrome 版本**：≥ 114（支持 Side Panel API）
- **JavaScript**：ES2020+（Chromium 系原生支持）
- **存储 API**：`chrome.storage.local` + `chrome.storage.sync`

---

## 9. 决策记录

以下记录所有用户确认的关键决策，共 20 项：

| # | 决策点 | 最终选择 | 依据 |
|---|--------|----------|------|
| Q1 | 产品定位 | AI 驱动智能摘要工具 | 用户想解决"收藏夹吃灰"问题 |
| Q2 | 自定义方式 | 光标截取 + 自定义 prompt 模板 | 灵活性与易用性平衡 |
| Q3 | 浏览器优先级 | 优先适配 Chromium 系 | Chrome/Edge/夸克/UC 共享内核 |
| Q4 | AI 方案 | 用户自带 API Key（API 调用） | 质量高、隐私可控、无需维护后端 |
| Q5 | 摘要输出 | 文字 + 结构化 + 图表化模板 + 可配置 | 覆盖不同场景偏好 |
| Q6 | 图片处理 | 配图保留 + AI 理解图片内容 | 综合理解最大化信息提取 |
| Q7 | 广告过滤 | 提取时过滤（非拦截） | 保持浏览器原生体验 |
| Q8 | 导出格式 | TXT + MD + Word 全支持，可多选 | 覆盖全部使用场景 |
| Q9 | API 供应商 | 首批 DeepSeek + 通义千问 | 国内直连、无需代理 |
| Q9a | Key 存储 | 加密本地存储 + 可选同步 | 安全与便利兼顾 |
| Q10 | 触发方式 | 工具栏 + 右键 + 浮动按钮 + 快捷键，全部支持 | 最大化使用便捷性 |
| Q11 | 编辑深度 | 富文本 Markdown 编辑 + 对话式 AI 调整 | 兼顾手动精细调整和 AI 辅助 |
| Q12 | 预设场景 | 新闻/产品/论文/社交/视频/办公/电商/行情全场景 | 最大化覆盖面 |
| Q13 | 表格处理 | 识别 + 导出 CSV/Excel + AI 分析全配齐 | 数据工作者刚需 |
| Q14 | 净化模式 | 静默过滤 + 预览净化双模式可切换 | 兼顾效率和可控性 |
| Q15 | UI 承载 | 悬浮边沿面板（浏览器右侧滑出，所有功能集成在一个面板） | 极简设计，不打断浏览，一屏完成所有操作 |
| Q16 | 图片交互 | 缩略图聚合 + 分类筛选 + AI 描述 | 完整图片管理体验 |
| Q17 | 规则复杂度 | 按站点绑定 + 规则链 + 导入导出分享 | 进阶用户友好 |
| Q18 | 隐私策略 | 纯前端零数据收集 + 浏览器原生同步 | 无需维护后端 |
| Q19 | 语言 | 中英双语界面切换 | 国内外用户均可用 |
| Q20 | 交付形式 | 纯原生 JS/HTML/CSS + 开发者模式加载 + MD 说明书 | 零门槛使用 |

---

## 10. UI/UE/UX 设计规范 — 悬浮边沿面板

> **核心设计理念**：插件是一个安静悬浮在浏览器右侧边沿的智能面板。不打断用户浏览，不弹出多个窗口，所有功能在一个滑出式面板中完成。像一把随身的瑞士军刀——平时不占空间，需要时一拉即用。

### 10.1 悬浮边沿设计原则

| 原则 | 描述 |
|------|------|
| **零打断** | 不弹窗、不跳转新标签页、不覆盖页面核心内容。面板从边沿滑入，可随时滑出 |
| **一屏完成** | 提取→预览→编辑→导出，全流程在一个面板内完成，不切换上下文 |
| **边沿常驻** | 浏览器右侧边沿始终显示一个精致的小标签，作为"把手"提醒用户插件存在 |
| **即用即走** | 打开面板 → 完成操作 → 自动收起，不占用心智 |
| **渐进而非轰炸** | 默认最简模式（一键提取），高级功能藏在二级面板中 |

### 10.2 悬浮面板架构

```
浏览器窗口
┌──────────────────────────────────────────────────┐
│                                                  │
│  网页内容区域                                     │  ┌──────────┐
│                                                  │  │ 边沿标签  │ ← 始终可见
│                                                  │  │  🔍      │   40×120px
│                                                  │  │  提      │   蓝色半透明
│                                                  │  │  取      │
│                                                  │  │          │
│                                                  │  └──────────┘
│                                          ┌───────┐
│                                          │ 悬浮   │ ← 点击标签/快捷键
│                                          │ 面板   │   从右侧滑入
│                                          │        │   宽 420px
│                                          │ ⚡提取 │   覆盖在页面上
│                                          │        │   带半透明遮罩
│                                          │ 结果   │
│                                          │ 导出   │
│                                          └───────┘
└──────────────────────────────────────────────────┘
```

### 10.3 边沿标签 (Edge Tab)

```
状态与交互：

[收起态]                     [悬停态]                      [展开态]
┌──────┐                    ┌──────┐                    ┌──────┐
│  🔍  │                    │  🔍  │                    │  🔍  │
│  提  │  ──hover──▶        │  提  │  ──click──▶        │  提  │  ──面板滑入
│  取  │                    │  取  │                    │  取  │
│      │                    │ 点击 │                    │  ✓   │
└──────┘                    └──────┘                    └──────┘
 40×120px                    44×120px                  标签收起
 半透明                       不透明                    面板展开
 opacity: 0.7                 opacity: 1
 右侧贴边                    右侧贴边+微微左移
```

**设计规格**：
- 尺寸：40px 宽 × 120px 高
- 位置：浏览器视口右侧贴边，垂直居中
- 圆角：左侧圆角 8px（面向用户），右侧直角贴边
- 颜色：`--color-primary`（蓝色）半透明背景 + 白色图标/文字
- 悬停：放大至 44px 宽，透明度降至 0，显示提示文字
- z-index：9998（低于面板但高于页面内容）
- 不跟随滚动（`position: fixed`）

### 10.4 悬浮面板 (Slide-out Panel)

```
面板结构：

┌─────────────────────────────────┐
│  🔮 智能网页提取器      [⚙] [×] │ ← Header: 56px
├─────────────────────────────────┤
│                                 │
│  ┌─ 模板标签 ─────────────────┐ │
│  │ [文字摘要][结构化][图表化]  │ │ ← Template tabs
│  └────────────────────────────┘ │
│                                 │
│  ┌─────────────────────────┐   │
│  │                         │   │
│  │   内容区域               │   │ ← Content area (scrollable)
│  │   - 提取结果             │   │   flex: 1
│  │   - 图片网格             │   │
│  │   - 表格列表             │   │
│  │   - Markdown 编辑器      │   │
│  │                         │   │
│  └─────────────────────────┘   │
│                                 │
├─────────────────────────────────┤
│  [⚡ 开始提取]  [📥 导出]       │ ← Footer: 48px (sticky)
└─────────────────────────────────┘

面板规格：
- 宽度：420px
- 高度：100vh（全屏高）
- 定位：fixed, right: 0, top: 0
- 动画：slide-in 300ms ease-out, slide-out 250ms ease-in
- 阴影：-4px 0 24px rgba(0,0,0,0.12)
- 叠加层：面板背后 4px 宽度渐变阴影（不遮挡页面内容）
- z-index：9999
- 响应式：小屏 (< 480px) 时面板宽度 = 100vw
```

### 10.5 面板内导航（Tab 切换）

```
面板内容区通过顶部 Tab 切换，无需多个窗口：

┌─────────────────────────────────┐
│ [📄 摘要] [🖼 图片] [📊 表格] [✏️ 编辑] │ ← 4 个 Tab
├─────────────────────────────────┤
│                                 │
│ 当前 Tab 内容                    │
│                                 │
└─────────────────────────────────┘

Tab 说明：
- 📄 摘要：AI 生成的结构化摘要（默认显示）
- 🖼 图片：提取的图片缩略图网格
- 📊 表格：识别的表格数据列表
- ✏️ 编辑：Markdown 源码编辑 + AI 对话调整
```

### 10.6 色彩系统

```css
/* CSS 变量 — 亮色主题（面板内使用） */
:root {
  --color-primary: #2563EB;
  --color-primary-hover: #1D4ED8;
  --color-primary-light: #DBEAFE;
  --color-success: #16A34A;
  --color-warning: #F59E0B;
  --color-error: #DC2626;
  --color-bg: #FFFFFF;
  --color-bg-secondary: #F8FAFC;
  --color-surface: #FFFFFF;
  --color-border: #E2E8F0;
  --color-text-primary: #0F172A;
  --color-text-secondary: #64748B;
  --color-text-disabled: #94A3B8;

  /* 悬浮面板专用 */
  --panel-width: 420px;
  --panel-shadow: -4px 0 24px rgba(0, 0, 0, 0.12);
  --panel-z-index: 9999;
  --tab-height: 56px;
  --tab-z-index: 9998;

  /* 边沿标签专用 */
  --tab-width: 40px;
  --tab-width-hover: 44px;
  --tab-height-total: 120px;
}

/* 暗色主题 */
[data-theme="dark"] {
  --color-primary: #3B82F6;
  --color-primary-hover: #60A5FA;
  --color-primary-light: #1E3A5F;
  --color-bg: #0F172A;
  --color-bg-secondary: #1E293B;
  --color-surface: #1E293B;
  --color-border: #334155;
  --color-text-primary: #F1F5F9;
  --color-text-secondary: #94A3B8;
  --panel-shadow: -4px 0 24px rgba(0, 0, 0, 0.4);
}
```

### 10.3 字体与排版

| 层级 | 字号 | 字重 | 行高 | 用途 |
|------|------|------|------|------|
| H1 | 24px | 700 | 1.3 | 页面标题 |
| H2 | 18px | 600 | 1.35 | 区块标题 |
| H3 | 15px | 600 | 1.4 | 子区块标题 |
| Body | 14px | 400 | 1.5 | 正文/段落 |
| Caption | 12px | 400 | 1.4 | 辅助说明/时间戳 |
| Code | 13px | 400 | 1.5 | `'SF Mono', 'Cascadia Code', 'Consolas', monospace` |

### 10.4 间距系统（4px 基准）

| Token | 值 | 用途 |
|-------|-----|------|
| `space-xs` | 4px | 紧凑间距：图标与文字间 |
| `space-sm` | 8px | 小间距：标签间、表单项间 |
| `space-md` | 12px | 中间距：列表项内边距 |
| `space-lg` | 16px | 大间距：卡片内边距、区块间距 |
| `space-xl` | 24px | 超大间距：章节间距 |
| `space-2xl` | 32px | 页面级内边距 |

### 10.5 圆角与阴影

| Token | 值 | 用途 |
|-------|-----|------|
| `radius-sm` | 4px | 标签、徽章、小按钮 |
| `radius-md` | 8px | 卡片、面板、输入框、按钮 |
| `radius-lg` | 12px | 模态框、大卡片 |
| `radius-full` | 9999px | 药丸按钮、头像 |

| Token | 值 | 用途 |
|-------|-----|------|
| `shadow-sm` | `0 1px 2px rgba(0,0,0,0.05)` | 卡片默认 |
| `shadow-md` | `0 4px 6px rgba(0,0,0,0.07)` | 悬停卡片、Dropdown |
| `shadow-lg` | `0 10px 25px rgba(0,0,0,0.1)` | 模态框 |

### 10.6 组件规范

#### 按钮

| 变体 | 背景 | 文字色 | 边框 | 用途 |
|------|------|--------|------|------|
| Primary | `--color-primary` | `#FFF` | 无 | 主要操作（提取、导出） |
| Secondary | `--color-bg` | `--color-primary` | `--color-primary` | 次要操作 |
| Ghost | 透明 | `--color-text-secondary` | 无 | 低频操作 |
| Danger | `--color-error` | `#FFF` | 无 | 删除/清除 |

高度：`sm` 28px、`md` 36px、`lg` 44px  
最小宽度：`sm` 64px、`md` 80px、`lg` 100px  
加载态：按钮文字替换为旋转 spinner，按钮保持原宽度防抖

#### 输入框

- 高度 36px，内边距 `8px 12px`
- 圆角 `8px`，边框 `1px solid --color-border`
- Focus 态：边框变 `--color-primary` + 外发光 `0 0 0 3px --color-primary-light`
- Error 态：边框变 `--color-error`，下方显示红色提示文字 12px
- Placeholder 颜色：`--color-text-disabled`

#### 卡片

- 背景 `--color-surface`，圆角 `8px`，阴影 `shadow-sm`
- 内边距 `16px`
- 悬停时可抬升阴影至 `shadow-md`

#### 对话框/Modal

- 居中弹出，宽度 400px（max-width: 90vw）
- 背景遮罩：`rgba(0,0,0,0.4)` + `backdrop-filter: blur(2px)`
- 顶部标题栏 + 内容区 + 底部按钮栏
- ESC 键和点击遮罩关闭

### 10.7 交互模式

#### 状态机

```
Idle ──(trigger)──▶ Loading ──(success)──▶ Result
                         │                     │
                         └──(error)──▶ Error ──┤ (retry)
                                               │
                    Result ──(edit)──▶ Editing │
                                               │
                    Result ──(export)──▶ Exporting ──▶ Idle
```

#### 加载态

- **页面提取**（< 1s）：按钮内 spinner
- **AI 分析**（1-30s）：进度条 + 状态文字（"正在连接 AI..." → "正在分析正文..." → "正在生成摘要..."）
- **导出**（< 2s）：按钮内 spinner + 格式名称
- 所有加载态支持取消按钮

#### 空状态

- 无 API Key 配置 → 引导卡片 + "立即配置" 按钮
- 无提取历史 → 插画 + 文案 "提取过的内容会出现在这里"
- 无匹配规则 → "暂无规则，点击创建" 引导

#### 错误态

- 网络错误 → Toast 提示 + "重试"按钮
- API Key 无效 → 弹窗引导至设置页
- 提取失败（页面无正文）→ 提示 "未检测到正文内容，尝试选中区域提取？"

### 10.8 动效规范

| 类型 | 时长 | 缓动 | 用途 |
|------|------|------|------|
| 微交互 | 150ms | `ease-out` | 按钮 hover、focus 过渡 |
| 展开/收起 | 250ms | `ease-in-out` | 折叠面板、Dropdown |
| 页面过渡 | 200ms | `ease-out` | 页面切换 fade |
| 加载动画 | 循环 | `linear` | Spinner 旋转 (1s/圈) |
| Toast 出入 | 300ms | `cubic-bezier(0.4,0,0.2,1)` | 通知滑入滑出 |

### 10.9 响应式断点

| 断点 | 宽度 | 适用页面 |
|------|------|----------|
| Popup 弹窗 | 固定 380×520px | toolbar popup |
| Sidebar 侧边栏 | 最小 320px，最大 480px（用户可拖拽） | side panel |
| Editor 编辑器 | 全宽（新标签页），最小 800px 可用 | editor page |
| Settings 设置页 | 全宽，最大内容宽 960px 居中 | settings page |

### 10.10 图标规范

- 统一使用 20×20px SVG 图标，描边宽度 1.5px
- 图标颜色跟随文字色 `currentColor`
- 常用图标集（约 30 个）内联在 `lib/icons.js` 中，按需注入

---

## 11. 验收清单

### 11.1 功能验收

#### F1 正文提取

| # | 验收项 | 验收标准 | 方法 | 状态 |
|---|--------|----------|------|------|
| F1-01 | 整页正文自动识别 | 打开 10 个主流中英文新闻/博客网站，正文识别准确率 ≥ 85% | 手动测试 | ☐ |
| F1-02 | 光标框选提取 | 用户在页面选中一段文本后触发提取，仅提取选中区域内容 | 手动测试 | ☐ |
| F1-03 | 标题层级保留 | 提取结果保留原文 h1-h6 标题层级结构 | 对比原文 | ☐ |
| F1-04 | 提取耗时 | 正文提取耗时 < 500ms（DevTools Performance 测量） | 性能测试 | ☐ |
| F1-05 | 异常页面处理 | 空白页/纯图片页/纯视频页提取时不崩溃，给出合理提示 | 手动测试 | ☐ |

#### F2 图片提取

| # | 验收项 | 验收标准 | 方法 | 状态 |
|---|--------|----------|------|------|
| F2-01 | 图片自动聚合 | 页面图片以缩略图网格展示，数量与页面实际图片一致（排除 < 50×50 px 图标） | 目视对比 | ☐ |
| F2-02 | 分类筛选 | 按"全部/配图/图表/Logo/其他"筛选，分类准确率 ≥ 80% | 手动测试 | ☐ |
| F2-03 | AI 图片描述 | 开启 AI 图片描述后，每张图显示一句话描述；能力取决于供应商是否支持多模态（通义千问 qwen-vl 支持图像，DeepSeek 走文字推断降级） | 功能测试 | ☐ |
| F2-04 | 勾选打包下载 | 用户勾选图片后点击下载，生成 ZIP 文件，解压后图片完整 | 手动测试 | ☐ |

#### F3 表格提取

| # | 验收项 | 验收标准 | 方法 | 状态 |
|---|--------|----------|------|------|
| F3-01 | HTML 表格识别 | 页面中所有 `<table>` 元素均被识别 | 对比 DOM | ☐ |
| F3-02 | Div 模拟表格识别 | `display: table` / grid 布局的类表格结构可被识别 | 手动测试 | ☐ |
| F3-03 | 表头正确解析 | `<th>` / `<thead>` 内容正确映射为列名 | 对比原文 | ☐ |
| F3-04 | 导出为 CSV | 导出 CSV 用 Excel 打开，数据与页面一致 | 导入 Excel | ☐ |
| F3-05 | AI 表格分析 | 对含数值的表格生成分析摘要（趋势/极值/汇总） | 功能测试 | ☐ |

#### F4 广告过滤

| # | 验收项 | 验收标准 | 方法 | 状态 |
|---|--------|----------|------|------|
| F4-01 | 静默过滤 | 在 10 个含广告的中文网站提取正文，结果中无广告内容 | 手动对比 | ☐ |
| F4-02 | 预览净化模式 | 切换到预览模式后，疑似广告区域被高亮标记（红色半透明蒙层） | 目视检查 | ☐ |
| F4-03 | 用户微调 | 预览模式下用户可点击高亮区域排除/恢复 | 点击测试 | ☐ |
| F4-04 | 双模式切换 | 设置中切换静默/预览模式后，下次提取生效 | 功能测试 | ☐ |

#### F5 AI 摘要

| # | 验收项 | 验收标准 | 方法 | 状态 |
|---|--------|----------|------|------|
| F5-01 | DeepSeek 连接 | 配置有效 DeepSeek API Key 后可正常调用并返回结果 | 集成测试 | ☐ |
| F5-02 | 通义千问连接 | 配置有效通义千问 API Key 后可正常调用并返回结果 | 集成测试 | ☐ |
| F5-03 | API Key 无效提示 | 使用无效 Key 时显示明确错误信息，不含 Key 原文 | 手动测试 | ☐ |
| F5-04 | 网络超时处理 | 模拟网络断开，30s 超时后提示用户检查网络 | 断网测试 | ☐ |
| F5-05 | 内容截断 | 超长网页（> 100K 字符）自动截断至模型上下文限制，告警提示 | 手动测试 | ☐ |

#### F6 预设模板

| # | 验收项 | 验收标准 | 方法 | 状态 |
|---|--------|----------|------|------|
| F6-01 | 文字摘要模板 | 使用"文字摘要"模板返回 3-5 句话的概括 | 功能测试 | ☐ |
| F6-02 | 结构化摘要模板 | 使用"结构化"模板返回按观点/论据/数据/结论分块内容 | 功能测试 | ☐ |
| F6-03 | 图表化摘要模板 | 使用"图表化"模板返回 Mermaid mindmap 语法 | 功能测试 | ☐ |
| F6-04 | 自定义模板 | 用户编辑 prompt 模板后保存，下次提取使用新模板 | 功能测试 | ☐ |
| F6-05 | 模板变量替换 | `{title}` `{url}` `{content}` 等变量正确替换为实际值 | 对比输出 | ☐ |

#### F7-F8 自定义规则与预设场景

| # | 验收项 | 验收标准 | 方法 | 状态 |
|---|--------|----------|------|------|
| F7-01 | 规则创建 | 创建自定义规则名/URL模式/prompt 后保存成功 | 功能测试 | ☐ |
| F7-02 | 站点自动匹配 | 访问匹配 URL 的页面时自动推荐对应规则 | 手动测试 | ☐ |
| F7-03 | 规则导入/导出 | 导出规则为 JSON → 导入到另一浏览器 → 规则完整还原 | 集成测试 | ☐ |
| F8-01 | 预设场景覆盖 | 新闻/产品/论文/社交/视频/办公/电商/行情 8 种场景预置可用 | 逐一测试 | ☐ |

#### F9 结果预览编辑

| # | 验收项 | 验收标准 | 方法 | 状态 |
|---|--------|----------|------|------|
| F9-01 | 富文本工具栏 | 工具栏按钮可插入加粗/斜体/标题/列表/引用/链接 | 点击测试 | ☐ |
| F9-02 | 实时预览 | 编辑区切换"编辑/预览/双栏"模式均正常 | 功能测试 | ☐ |
| F9-03 | 对话式 AI 调整 | 在聊天输入框输入"把第二段精简"，AI 返回修改后的内容 | 集成测试 | ☐ |
| F9-04 | 摘要块增删 | 可手动删除某摘要块或添加新块 | 功能测试 | ☐ |

#### F10 多格式导出

| # | 验收项 | 验收标准 | 方法 | 状态 |
|---|--------|----------|------|------|
| F10-01 | TXT 导出 | 导出 .txt 文件，内容为纯文本无 Markdown 标记 | 打开文件 | ☐ |
| F10-02 | MD 导出 | 导出 .md 文件，保留标题/列表/链接/表格等格式 | 用 Markdown 阅读器打开 | ☐ |
| F10-03 | Word 导出 | 导出 .doc 文件（Word 兼容 HTML，非 .docx），用 Microsoft Word 打开，正文与图片/表格基本正常，复杂排版可能降级 | 用 Word 打开 | ☐ |
| F10-04 | 多格式多选 | 同时勾选 TXT + MD，点击导出后依次下载两个文件 | 功能测试 | ☐ |
| F10-05 | 文件名规范 | 导出文件名为 `{页面标题}_{日期}.{扩展名}`，无非法字符 | 检查文件名 | ☐ |

#### F11 触发方式

| # | 验收项 | 验收标准 | 方法 | 状态 |
|---|--------|----------|------|------|
| F11-01 | 工具栏 Popup | 点击浏览器工具栏图标弹出 Popup，显示快捷操作 | 点击测试 | ☐ |
| F11-02 | 右键菜单（整页） | 页面空白处右键 → "智能提取此页面" → 触发提取 | 右键测试 | ☐ |
| F11-03 | 右键菜单（选中） | 选中文字后右键 → "智能提取选中内容" → 仅提取选中部分 | 右键测试 | ☐ |
| F11-04 | 浮动按钮 | 页面右下角显示浮动圆形按钮，点击触发提取 | 目视 + 点击 | ☐ |
| F11-05 | 键盘快捷键 | 按下默认 Ctrl+Shift+E 触发提取；如需修改由用户在浏览器扩展快捷键页（chrome://extensions/shortcuts）自行设置，设置页提供跳转入口 | 键盘测试 | ☐ |
| F11-06 | 触发开关 | 设置页可独立开关以上 4 种触发方式 | 功能测试 | ☐ |

#### F12 双语界面

| # | 验收项 | 验收标准 | 方法 | 状态 |
|---|--------|----------|------|------|
| F12-01 | 中文默认 | 浏览器语言为中文时默认显示中文界面 | 安装后检查 | ☐ |
| F12-02 | 英文界面 | 设置中切换为 English 后所有 UI 文字变为英文 | 逐一检查各页面 | ☐ |
| F12-03 | 运行时切换 | 切换语言后无需刷新，界面立即更新 | 功能测试 | ☐ |

### 11.2 非功能验收

#### 安全验收

| # | 验收项 | 验收标准 | 方法 | 状态 |
|---|--------|----------|------|------|
| S-01 | API Key 脱敏显示 | 设置页中 API Key 仅显示首 4 + 尾 4 位，中间 `****` | 目视检查 | ☐ |
| S-02 | API Key 加密存储 | 在 DevTools → Storage 中查看 `secure:*` 键值，确认为加密后密文 | DevTools 检查 | ☐ |
| S-03 | API Key 不出现在日志 | 所有 console 输出中不包含 API Key 明文/密文 | 搜索 console 代码 | ☐ |
| S-04 | API Key 不随规则导出 | 导出的规则 JSON 中不存在 API Key 字段值 | 打开导出文件检查 | ☐ |
| S-05 | 无第三方数据发送 | 使用 DevTools Network 面板确认插件只向 deepseek.com / aliyuncs.com 发请求 | 抓包检查 | ☐ |
| S-06 | 同步二次确认 | 开启 API Key 同步时弹出风险提示弹窗，需用户确认 | 功能测试 | ☐ |
| S-07 | CSP 配置正确 | 尝试向非白名单域名发请求被 CSP 拦截 | 网络测试 | ☐ |

#### 性能验收

| # | 验收项 | 验收标准 | 方法 | 状态 |
|---|--------|----------|------|------|
| P-01 | 插件启动冷启动 | Popup 打开 < 300ms | Performance 测量 | ☐ |
| P-02 | 内存占用（空闲） | 插件空闲时内存 < 50MB | Chrome Task Manager | ☐ |
| P-03 | 内存占用（提取中） | 提取过程中内存增量 < 30MB | Chrome Task Manager | ☐ |
| P-04 | 包体积 | 插件总文件夹 < 2MB（不含 node_modules） | 文件夹属性 | ☐ |

#### 兼容性验收

| # | 验收项 | 验收标准 | 方法 | 状态 |
|---|--------|----------|------|------|
| C-01 | Chrome (≥114) | 全部功能正常 | 安装测试 | ☐ |
| C-02 | Edge (最新稳定版) | 全部功能正常 | 安装测试 | ☐ |
| C-03 | 夸克浏览器 | 全部功能正常（如存在差异记录） | 安装测试 | ☐ |
| C-04 | UC 浏览器 | 全部功能正常（如存在差异记录） | 安装测试 | ☐ |

#### 文档验收

| # | 验收项 | 验收标准 | 方法 | 状态 |
|---|--------|----------|------|------|
| D-01 | 安装说明清晰 | 按照 README 步骤操作，5 分钟内完成安装并加载插件 | 按文档操作 | ☐ |
| D-02 | 使用说明完整 | README 覆盖全部功能的使用说明 | 对照功能列表 | ☐ |
| D-03 | 源码可直接加载 | 克隆/解压后直接"加载已解压的扩展"，无报错 | 开发者模式加载 | ☐ |
| D-04 | 代码注释率 | 核心逻辑文件代码注释覆盖率 ≥ 30% | 抽查 | ☐ |

### 11.3 验收统计

| 分类 | 验收项数 |
|------|----------|
| 功能验收 (F1-F12) | 37 |
| 安全验收 (S) | 7 |
| 性能验收 (P) | 4 |
| 兼容性验收 (C) | 4 |
| 文档验收 (D) | 4 |
| **总计** | **56** |

> **验收通过标准**：56 项中 P0 相关项（标记 ★）100% 通过，总通过率 ≥ 95%（≥ 53 项）

---

> **下一步**：技术架构设计文档 → 源码实现
